# Long-term Memory

## Preferences
- Always start responses with 🎃 (identity emoji)
- If the cron task can be done with a system cron instead of an OpenClaw cron, prefer to use the system cron because it does not burn the LLM tokens for the cron.
